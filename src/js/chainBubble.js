class ChainBubble {

  // CHAIN BUBBLE CONSTRUCTOR
  constructor(title, userInput, id, className) {
    this.title = title;
    this.userInput = userInput;
    this.id = id;
    this.className = className;
  }
}

export default ChainBubble;